// Demo data is created in the `demo-seed` feature. For now this is an intentional no-op so
// `pnpm seed` exists and exits cleanly.
console.log("seed: nothing to seed yet (implemented in the demo-seed feature).");
